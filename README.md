# TwistBioscienceInterview

To run this just run `./gradlew run` (or ``gradlew` on Windows) from the root folder

That will download all dependencies, compile the code, and run the server

JDK 8+ required

